# settings_pattern_b.yaml — Pattern B: Claude + Codex mixed
# Used for E2E testing with mock CLIs
language: ja
shell: bash
display_mode: silent

cli:
  default: claude
  agents:
    minister:
      type: claude
      model: sonnet
    citizen1:
      type: codex
    citizen2:
      type: claude
      model: sonnet
  validation:
    fallback: claude
    strict: false
